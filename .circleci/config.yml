version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  release:
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm run prepublishOnly
            - run: npm run release
workflows:
  release:
    jobs:
      - release
